import React from "react";
import PropTypes from "prop-types";

const NotFound = (props) => {
  return (
    <>
      <h1>Not Found</h1>
    </>
  );
};

NotFound.propTypes = {};

export default NotFound;
